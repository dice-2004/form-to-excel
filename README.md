# form to excel app
It uses xlwings, so it cannot be used on Ubuntu or other Linux systems.

**form(flask)**  ➪  **python(xlwings)**  ➪  **excel**

# why create this
To facilitate real-time Q&A from participants during study sessions.
